# Repository instructions

- Use `$create-darles-portfolio-card` for adding or updating Darles Games portfolio products.
- Preserve the live `projects.js` schema and never invent public facts.
- Store covers as 1600×900 WebP files in the established cover directory.
- Commit and push portfolio changes only after all mandatory checks pass.
- Never include unrelated user changes in a commit.
